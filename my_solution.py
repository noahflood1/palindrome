'''
A function that takes a string of characters as input and returns a boolean where:

False - indcates that the string is not a palindrome.
True - indicates that the string is a palindrome.
'''
def is_palindrome(string):
   return False